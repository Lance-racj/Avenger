<view class="idleCt">
  <!-- 搜索框 -->
  <searchInput bindtap="toSearch" />
  <!-- 物品展示区域 -->
  <view class="idle"> 
    <idleCard 
      wx:for="{{idleList}}" 
      wx:key="index" 
      idleItem="{{item}}" 
      bindtap="goToDetail"
      data-item="{{item}}"
    />
  </view>
</view>
