<view class="helpCt">
  <!-- 搜索框 -->
  <searchInput bindtap="toSearch" />
  <!-- 物品展示区域 -->
  <view class="idle"> 
    <helpCard 
      wx:for="{{list}}" 
      wx:key="index" 
      helpItem="{{item}}" 
      bindtap="goToHelpDetail"
      data-item="{{item}}"
    />
  </view>
</view>
