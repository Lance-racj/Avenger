<view class="myPublishLost">
  <Tabs tabList="{{tabList}}" bindgetTab="getTab"></Tabs>
  <view class="lose"> 
    <lostCard wx:for="{{lostList}}" wx:key="index" lostItem="{{item}}" />
  </view>
</view>
